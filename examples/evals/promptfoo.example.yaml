# 示例：promptfoo eval dry-run recipe。
recipe_id: promptfoo.demo-v0.1
backend: promptfoo
stage: suite

data:
  config: examples/evals/promptfoo.config.example.yaml

outputs:
  output_dir: runs/eval_runs/promptfoo-demo-v0.1

gate:
  min_pass_rate: 0.8
  allow_failed_cases: false
