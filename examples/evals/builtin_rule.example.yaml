# 示例：BadcaseFlow 内置 Agent trace 规则评测。
recipe_id: builtin.rule.demo-v0.1
backend: builtin
stage: agent_trace

data:
  traces: runs/demo/traces.jsonl
  suite: examples/datasets/eval_tasks.jsonl

outputs:
  output_dir: runs/eval_runs/builtin-rule-demo-v0.1

gate:
  min_pass_rate: 0.8
  allow_failed_cases: false
